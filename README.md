# ethancooke.github.io

Minimal Jekyll landing-page setup.

## Update Jekyll Dependencies

Run:

```bash
./scripts/update-jekyll.sh
```

This updates Bundler plus the Jekyll/theme/plugin gems to the latest versions
allowed by `Gemfile`, then runs a local `jekyll build` check.
